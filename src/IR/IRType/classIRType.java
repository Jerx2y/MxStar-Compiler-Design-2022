package IR.IRType;

import IR.IRClass;

public class classIRType extends IRType {
    int bytes;

    public classIRType(IRClass ic) {
        ;
    }

    @Override
    public int getBytes() {
        return bytes;
    }
}
